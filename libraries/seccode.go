package libraries

/*func Generatebase64(width, height int, key string) (captchaId, base64Png string) {
	var configD = base64Captcha.ConfigDigit{
		Height:     height,
		Width:      width,
		MaxSkew:    0.65,
		DotCount:   75,
		CaptchaLen: 4,
	}
	captchaId, digitCap := base64Captcha.GenerateCaptcha("", configD)
	base64Png = base64Captcha.CaptchaWriteToBase64Encoding(digitCap)
	return captchaId, base64Png

}
func Generate(width, height int, key string) (captchaId string, binary []byte) {
	var configD = base64Captcha.ConfigDigit{
		Height:     height,
		Width:      width,
		MaxSkew:    0.65,
		DotCount:   75,
		CaptchaLen: 4,
	}
	captchaId, digitCap := base64Captcha.GenerateCaptcha("", configD)
	return captchaId, digitCap.BinaryEncoding()
}

// base64Captcha verify http handler
func Verify(key, id string) bool {
	return base64Captcha.VerifyCaptcha(id, key)

}*/
